export enum UserIconModes {
    Small,
    Large
}

export enum MenuItemTypes {
    Dashboard,
    Projects
}

export enum IconButtonTypes {
    Arrow,
    Points
}

export enum LogoColors {
    Orange = 'orange',
    White = 'white',
    Black = 'black'
}